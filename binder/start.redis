#!/bin/bash


nohup  redis-server --daemonize yes --loadmodule $REDIS_HOME/RedisTimeSeries/bin/linux-x64-release/redistimeseries.so &> ~/logs/redis-$REDIS_VERSION.log &

sleep 10

redis-cli ping

cd ~/